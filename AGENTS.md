# job-portal-backend

A NestJS REST API for a startup job portal. See [SPEC.md](./SPEC.md) for the v1 implementation spec.

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues in `ChinGuang/job-portal-backend`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
